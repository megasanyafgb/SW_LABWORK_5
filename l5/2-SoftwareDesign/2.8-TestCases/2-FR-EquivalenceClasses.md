# Класи еквівалентності для функції встановити_посаду()

| Опис вхідних умов | Правильний/Неправильний клас еквівалентності | Опис значень вхідних даних |
|------------------|---------------------------------------------|--------------------------|
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Правильний | номер_працівника = 5<br/>нова_посада = "Менеджер" |
| 1. номер_працівника <= 0<br/>2. номер_працівника існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = -5<br/>нова_посада = "Менеджер" |
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника НЕ існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = 9999<br/>нова_посада = "Менеджер" |
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника існує в БД<br/>3. нова_посада НЕ починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = 5<br/>нова_посада = "менеджер" |
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада < 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = 5<br/>нова_посада = "МН" |
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада > 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = 5<br/>нова_посада = "Старший-менеджер-продажу-регіону" |
| 1. номер_працівника > 0 і <= 9999<br/>2. номер_працівника існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить спеціальні символи | Неправильний | номер_працівника = 5<br/>нова_посада = "Менеджер@" |
| 1. номер_працівника > 9999<br/>2. номер_працівника НЕ існує в БД<br/>3. нова_посада починається з великої літери<br/>4. довжина нова_посада >= 3<br/>5. довжина нова_посада <= 20<br/>6. нова_посада містить тільки букви та дефіс | Неправильний | номер_працівника = 10000<br/>нова_посада = "Менеджер" |

